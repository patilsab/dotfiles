# ============================================================
# CYBERPUNK / BLACKARCH - BASH PROFILE
# Arch Linux + i3 + Terminator
# ============================================================

# Load interactive Bash configuration
if [[ -f "$HOME/.bashrc" ]]; then
    source "$HOME/.bashrc"
fi
